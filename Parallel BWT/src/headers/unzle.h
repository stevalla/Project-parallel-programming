#ifndef UNZLE_H
#define UNZLE_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "util.h"

Text zleDecoding(const Text);

void convertBinToDec(unsigned char *, unsigned char *, size_t *);


#endif
